# OpenAI (zorunlu)
OPENAI_API_KEY=sk-...

# Opsiyonel: Varsayılan model
OPENAI_MODEL=gpt-4o-mini

# Proje/çalışma alanı yolları (opsiyonel; kod içinde varsayılan da kullanılabilir)
# SOURCE_PROJECTS_DIR=/path/to/allowed/source/projects
# WORKSPACE_DIR=/path/to/this/project/workspace
